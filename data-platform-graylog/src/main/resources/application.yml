server:
  port: 8092

spring:
  application:
    name: data-platform-graylog
  datasource:
    url: jdbc:postgresql://localhost:5432/dataplatform
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

nacos:
  discovery:
    enabled: false
