server:
  port: 8082

spring:
  profiles:
    active: @profiles.active@
  application:
    name: data-platform-access

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
