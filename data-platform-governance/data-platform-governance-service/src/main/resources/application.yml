server:
  port: 8085

spring:
  profiles:
    active: @profiles.active@
  application:
    name: data-platform-governance

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
