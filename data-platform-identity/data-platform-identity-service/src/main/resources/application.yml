server:
  port: 8086

spring:
  profiles:
    active: @profiles.active@
  application:
    name: data-platform-identity

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
